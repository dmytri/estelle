{
  "watch1": {
    "scenarios": [
      "features/runnable-package.feature:The package exposes the estelle launch command",
      "features/runnable-package.feature:Estelle boots as the Captain Bonny from a directory without Estelle assets",
      "features/runnable-package.feature:The booted Captain runs on the shipped default model from a directory without Estelle assets"
    ]
  }
}
