{
  "watch1": {
    "scenarios": [
      "features/interactive-launch.feature:The started session registers a live command for each seat",
      "features/interactive-launch.feature:A seat command switches the active seat in the started session"
    ]
  },
  "watch2": {
    "scenarios": [
      "features/interactive-launch.feature:The started session resolves provider and model configuration from the operator's agent directory",
      "features/interactive-launch.feature:The started session records the conversation for later resume"
    ]
  }
}
