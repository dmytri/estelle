{
  "name": "dispatch-no-match",
  "version": "0.0.0",
  "description": "Real open-plugin fixture: a single Bash matcher no write tool call matches, so the shim allows the write",
  "hooks": "hooks/hooks.json"
}
