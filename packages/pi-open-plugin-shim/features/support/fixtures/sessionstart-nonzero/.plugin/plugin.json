{
  "name": "sessionstart-nonzero-fixture",
  "version": "0.0.0",
  "description": "Real open-plugin fixture: a SessionStart hook that exits non-zero must not block the session",
  "hooks": "hooks/hooks.json"
}
