{
  "name": "plugin-root-fixture",
  "version": "0.0.0",
  "description": "Real open-plugin fixture: its write hook command uses ${PLUGIN_ROOT} so the shim must resolve it to the plugin directory",
  "hooks": "hooks/hooks.json"
}
